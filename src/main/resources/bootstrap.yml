server:
  port: 8888
spring:
  cloud:
    config:
      server:
        native:
          search-locations: file:///${PCFG_PATH}
        encrypt:
          enabled: false
  profiles:
    active: native
